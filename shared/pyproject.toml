[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dost"
version = "0.1.0"
description = "Shared DOST protocol and metrics for agent templates"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["dost*"]
